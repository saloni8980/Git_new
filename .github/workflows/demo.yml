name: hello -world
on:
  workflow_dispatch:
  push:
    branches:
      - main

  
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-steps
        run: echo "Hello world" 
      - name: new step
        run: echo " hello from other world"
  
